ContextProgressBar 依赖的 providers 和 modes 数据是由兄弟组件 ModelSelectorPanel 加载的，但加载时机不确定，导致绿线先显示默认值 4096。

解决方案是在 ChatPanel 中统一加载这些数据，确保 ContextProgressBar 有正确的数据


问题看起来并非如此
而且我打印日志发现还是null和undefined

我发现问题了
两个东西根本不同步
ai判断完全错误

问题在于状态有问题
chatSlice能正确获取到当前选中的id,但是这个上下文进度条却去找modeSlice
这两个同名的状态，完全不是一码事！

前者归聊天面板用，后者归agent面板用

你这AI能不能靠谱点（