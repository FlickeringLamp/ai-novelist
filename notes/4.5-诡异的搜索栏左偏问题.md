# 问题描述：
看起来Tailwind CSS样式没问题
但是始终显示左偏

# 过程
1. 猜测是左侧的侧边栏遮住了搜索面板（×）
2. 猜测是布局有问题，比如flex的相关玄学问题（×）

# 根源
最终问题在于
AI写前端时，习惯性地不设置h，w（尤其是相对父容器的宽高设置）
而浏览器对输入框等东西有默认宽度
默认宽/高大于父容器就会出现不可预见的问题

# 解决方案：
手动添加宽高设置
要求AI写前端时，子组件必须有h，w设置，一般是相对父容器的百分比，如h-[80%]