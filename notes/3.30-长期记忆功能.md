# 当前的情况

**存储位置**：`backend/data/db/store.db`（SQLite 数据库）

**记忆写入**（第 169-186 行）：
- 触发条件：用户消息中包含"记住"/"remember"/"记录"关键词
- 存储内容：用户的完整消息原文
- 命名空间：`("memories", user_id)`
- 实现方式较为简单，直接将整句话存储为记忆

```python
if "记住" in content or "remember" in content or "记录" in content:
    store.put(namespace, memory_id, {"data": memory_content})
```

**记忆检索**（第 115-137 行）：
- 检索时机：每次调用 LLM 前
- 检索方式：使用用户最新消息作为查询，语义搜索相关记忆
- 检索数量：最多 5 条
- 注入方式：将记忆内容添加到系统环境信息中


# 代码位置

- 记忆写入：[`backend/ai_agent/core/graph_builder.py:169-186`](backend/ai_agent/core/graph_builder.py:169)
- 记忆检索：[`backend/ai_agent/core/graph_builder.py:115-137`](backend/ai_agent/core/graph_builder.py:115)
- 存储初始化：[`backend/ai_agent/core/graph_builder.py:337-340`](backend/ai_agent/core/graph_builder.py:337)


================
思考：
如何添加长期记忆？
1. 看看cherrystudio怎么做的
2. 可能要重构一下用户id的问题

没用
可能linux的有问题？
再试试嵌入模型

换windows后没问题了
但是还是rag知识库那种搜索？

cherrystudio智能更新机制（最大区别）
全局记忆使用 LLM 进行智能记忆管理（memory-prompts.ts）：

ADD：新信息不存在时添加
UPDATE：信息存在但需更新（如"喜欢奶酪披萨"→"喜欢鸡肉披萨"）
DELETE：信息矛盾时删除
NONE：信息已存在，无需操作
这意味着全局记忆会自动去重、解决冲突、合并信息。

大概就是用AI管理rag,颗粒度更细